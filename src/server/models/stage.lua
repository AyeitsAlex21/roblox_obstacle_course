local StageModel = {
    stage_id = "", -- str
    seed = "", -- str
    num_checkpoints = 0, -- int
    obstacle_course = {}, -- serialized obstacle course
    created_on = "", -- date object
}

return StageModel
