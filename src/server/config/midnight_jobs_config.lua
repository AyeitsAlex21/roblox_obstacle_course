local MIDNIGHT_JOBS_CONFIG = {
    days_ahead = 30, -- Number of days ahead to generate obstacle courses from current date
    overwrite_existing = true -- overwrite already existing stages
}

return MIDNIGHT_JOBS_CONFIG